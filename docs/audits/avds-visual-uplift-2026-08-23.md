# ShadowBroker AVDS visual uplift — final local candidate

Date: 2026-08-23. Mode: visual uplift. Live AVDS inspection reported `4.7.0`
and source SHA `79342b07b061938c14101a213d1dd0c7a412d689`; its exact source
object is unavailable in this checkout. This remains a static semantic adapter,
not a package-consumption or source-current claim.

## Before → after decisions

1. **Public composition:** before evidence showed the research map mixed with
   operator panels, Time Machine actions, AI/market controls and a ticker. The
   local public candidate retains map layers, search, filters, news and health;
   it removes control-plane and market surfaces before they render.
2. **Private boundaries as UI, not failed actions:** Time Machine is now a
   small explanatory surface — “Recorded snapshots are available in the local
   operator runtime.” SAR, AI pins and Shodan are absent from the public layer
   chooser rather than presented as enabled-looking controls that fail later.
3. **Information hierarchy:** the factual health surface remains beside the
   page identity; its state is `degraded` when the real aggregate contract is
   not `ok`. The OSINT-map task is not obscured by a market ticker or a fake
   operational widget.

## AVDS mapping and browser evidence

- The semantic adapter continues to supply named canvas, surface, border, text,
  spacing and radius roles. `main h1`, service status, public Time Machine
  boundary and compact layer toggle are declared in the browser ledger.
- Compact evidence: `390x844`,
  `output/playwright/shadowbroker-local-candidate-2026-08-23/page-2026-08-23T17-23-53-231Z.png`.
- Desktop evidence: `1440x960`,
  `output/playwright/shadowbroker-local-candidate-2026-08-23/page-2026-08-23T17-14-13-190Z.png`.
- The compact and desktop snapshots each contain one H1, a visible health
  status, no System Settings/Operator shell/MKT/AI brief, and no console error
  or warning. Network evidence has no Time Machine, SAR, AI-pin, prediction or
  viewport-sync request.

The supplied AVDS browser runner is blocked locally because its own `playwright`
module is not resolvable. Manual Playwright CLI evidence is therefore recorded
as local verification, not as a runner-issued `browser accepted` claim. The
anti-generative and visual-craft ledgers have no unresolved findings and pass
their supplied standalone validators.

## External boundary

The public AVDS alias is still an external stale 0/4 record for image digest
`sha256:a17c…`. Its owner must publish the project-built contract at
`/var/www/avds-badge-control/v1/shadowbroker.json` and prove the public alias
matches the actual deployed revision before adoption can be called covered.

**AVDS status:** код готов · local-only
